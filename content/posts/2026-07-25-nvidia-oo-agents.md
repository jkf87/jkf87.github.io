---
title: "에이전트를 파이썬 클래스 하나로 통합해버린 NVIDIA NOOA — 설계 철학 정리"
date: 2026-07-25
tags:
  - agent
  - framework
  - NVIDIA
  - LLM
  - harness
  - code-as-action
source_url: https://arxiv.org/abs/2607.20709
source: "arxiv"
github_url: https://github.com/NVIDIA-NeMo/labs-OO-Agents
draft: true
refactor_hub: harness-self-improve-23
refactor_status: queued
---

에이전트 개발의 고통은 "분할"에 있음. 프롬프트는 템플릿 파일에, 툴 정의는 JSON 스키마에, 콜백은 별도 코드에, 워크플로우는 그래프 설정에 흩어져 있음. NVIDIA의 NOOA는 이걸 "에이전트는 하나의 파이썬 객체다"라는 철학으로 통합함. 메서드는 액션, 필드는 상태, docstring은 프롬프트, 타입 어노테이션은 계약임.

1. 통찰은 단순함. 파이썬이 이미 충분히 좋은 추상화를 갖고 있다는 것. 클래스가 에이전트 정의, 메서드가 기능, asyncio가 동시성, `...`(ellipsis) 바디가 LLM 구동 루프, 일반 메서드 바디가 결정론적 파이썬 코드임. PyTorch가 강력한 런타임을 단순한 파이썬 API로 줬던 철학의 에이전트 버전임.

![NOOA 구현 예시](/images/2026-07-25-nvidia-oo-agents/fig-1-p2.png)

2. 실행 전략은 두 개임. Predict는 싱글샷으로 분류·추출에 쓰고, 반환 타입으로 검증 후 실패 시 로컬 재시도를 돌림. CodeAct는 모델이 파이썬 코드를 쓰고 실행하고 관찰하는 REPL(코드를 입력하면 즉시 결과를 보는 대화형 실행 환경) 세션으로, 타입 검증된 값을 return할 때까지 반복함.

3. 여기서 좋은 지점은 결정론적 코드와 에이전트 루프의 경계가 소스 코드에 그대로 보인다는 것임. `...` 바디는 LLM 루프, 일반 바디는 그냥 파이썬. 개발자와 모델이 같은 인터페이스를 공유함. 어떤 프레임워크 DSL도 새로 익힐 필요가 없음.

4. 컨텍스트 렌더링은 3개 영역임. Static block은 시스템 프롬프트처럼 불변이라 KV-cache(이전 토큰의 계산 결과를 저장해 재사용하는 캐시) 재사용을 극대화함. Event history는 타입화된 이벤트의 append-only 시퀀스. Dynamic block은 매 호출 전 재평가되는 TODO나 live state임. 3층을 캐시 효율 순서로 배치한 설계임.

![컨텍스트 렌더링](/images/2026-07-25-nvidia-oo-agents/fig-3-p6.png)

5. 가장 독특한 건 참조에 의한 전달임. CodeAct 메서드의 인자가 live 파이썬 객체로 들어감. 모델은 전체 값이 아니라 타입·길이·head/tail 샘플의 미리보기만 보고, 코드에서 `numbers[50:]`처럼 자유롭게 조작함. 컨텍스트 윈도우가 아니라 실행 환경의 용량이 처리 한계가 되는 구조임.

6. 이거 하나로 토큰 경제가 바뀜. 수백만 행 테이블도 프롬프트에 안 들어가고 코드로만 다뤄짐. 큰 데이터를 다루는 자동화 파이프라인에서 "프롬프트에 다 넣었다가 터지는" 실패를 구조적으로 막는 방향임.

7. 메모리도 객체 스코프로 해결함. 대화가 길어지면 typed 필드와 named context block을 상태로 유지하며 압축에서 제외함. MemGPT의 working context와 비슷한데 파일이 아니라 객체 필드 자체가 상태가 된다는 차이임.

![메모리 시스템](/images/2026-07-25-nvidia-oo-agents/fig-5-p10.png)

8. 성능은 프레임워크 논문치고 실용적임. SWE-bench Verified에서 공개 모델 기준 경쟁력 있는 패스율을 냈고, ARC-AGI-3(인터랙티브 환경 과제로 에이전트 능력을 측정하는 벤치마크)에선 멀티에이전트 월드 모델 시스템을 단일 에이전트 + 1페이지 스킬로 압축하면서도 score-cost Pareto frontier(점수-비용 트레이드오프에서 최적 균형을 이루는 경계선)를 개선함. 단순화가 손해가 아니었다는 증거임.

9. 논문의 실질 기여 하나는 14개 프레임워크 비교표임. Typed I/O, pass by reference, code as action, loop engineering, object state, harness APIs 여섯 역량 기준으로 LangGraph, Claude Agent SDK, OpenAI Codex, Google ADK, OpenClaw 등을 비교함. NOOA는 여섯 개를 단일 표면에서 전부 지원하는 최초를 주장함.

10. 실무 채점. 프레임워크를 갈아탈 필요까지는 없는데, 이 논문의 원칙은 바로 적용할 수 있음. 첫째, 프롬프트·툴·상태를 흩어놓지 말고 한 모듈에 두고 경계를 코드로 보이게 만들 것. 둘째, 큰 데이터는 미리보기 + 참조로 넘기고 코드로 조작하게 할 것. 셋째, 캐시 친화적 컨텍스트 배치(불변→append-only→volatile)를 지킬 것.

11. 특히 타입 어노테이션을 계약으로 쓰는 관점이 좋음. 모델의 출력을 반환 타입으로 검증하고 실패 시 재시도하는 루프가 프레임워크 레벨에서 기본이 되면, 자동화 파이프라인의 "모델이 이상한 걸 내놓음" 문제를 타입 시스템으로 잡게 됨.

12. 한계. 벤치마크가 프레임워크 자체의 우위라기보다 설계 일관성의 증거에 가깝고, 14개 프레임워크 비교도 각자의 목적이 다르니 표를 절대 점수로 읹으면 안 됨. 생태계와 도구 지원은 기존 프레임워크가 앞남.

13. 그래도 결론부의 방향은 기억할 만함. 진행은 더 큰 모델이나 더 나은 프롬프트만이 아니라 모델과 하네스의 공동 개발에서 온다는 것. 그리고 에이전트 최적화가 프롬프트 탐색을 넘어 에이전트 객체 전체의 재작성으로 나아가야 한다는 것. 인터페이스가 그 장소가 되어야 한다는 주장임.

원문: [arXiv:2607.20709](https://arxiv.org/abs/2607.20709)
